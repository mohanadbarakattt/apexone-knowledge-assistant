# Template attribution

The page shell, `styles.css`, and `template.js` are adapted from
[Start Bootstrap Simple Sidebar v6.0.6](https://github.com/StartBootstrap/startbootstrap-simple-sidebar),
MIT licensed. The source license is included as `LICENSE` in this directory.

The bundled stylesheet includes Bootstrap 5.2.3, also MIT licensed; its copyright
notice and license link are retained in the stylesheet header. No third-party
assets are loaded from a CDN when the UI runs.
